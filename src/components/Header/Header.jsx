import styles from './styles.module.scss';

const Header = () => {

  return (
    <header className={styles.header}>
      <h1 className={styles.header__title}>Планировщик задач</h1>
    </header>
  );
};

export default Header;
